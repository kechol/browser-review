{
  "description": "Injects pending browser-review annotations into the conversation as context.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/inject-pending.mjs\"",
            "timeout": 5,
            "statusMessage": "Checking for browser review comments"
          }
        ]
      }
    ]
  }
}
